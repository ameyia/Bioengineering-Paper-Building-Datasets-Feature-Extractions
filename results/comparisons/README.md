# Baseline versus proxy comparisons

`validation_graphs/` contains PNG and PDF figures comparing internal performance
and external positive detection.

The direct-comparison scripts use the intersection of peptide names found in both
external-validation sets. This shared-set restriction is necessary because the
baseline and proxy result folders do not otherwise have identical denominators.

