# Baseline results: no proxy peptides

`external_validation/` contains the current cleaned 10-seed internal and external
validation. `model_summaries/` and `results_by_dataset/` contain earlier consolidated
training results.

The presentation values 0.849 internal MCC and 78% external detection are derived
from `external_validation/`.

