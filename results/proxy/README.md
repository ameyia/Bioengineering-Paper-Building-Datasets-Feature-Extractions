# Proxy-integrated results

`external_validation/` contains the 10-seed results for models trained on the
cleaned proxy-integrated feature tables.

Interpret these beside the baseline only when the same external peptides and same
summary procedure are used. The scripts in `src/visualization/` explicitly intersect
the external peptide names before direct comparisons.

