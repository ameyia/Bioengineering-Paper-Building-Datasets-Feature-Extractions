# Mentor handoff archives

`mentor_handoff_2026-07-25/` is the dated package created before the repository was
reorganized. Its complete ZIP intentionally preserves the earlier paths for
provenance.

For current navigation and reproducible commands, use the repository-level
`README.md` and `docs/` files. Do not copy paths from the archived handoff back into
current scripts.

