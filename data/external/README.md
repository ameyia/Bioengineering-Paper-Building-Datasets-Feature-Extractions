# Earlier external prediction outputs

- `external_predictions/`: per-model S3/S5 prediction tables.
- `s3_predictions/`: earlier S3-only predictions.
- `s3_external_predictions*.tsv`: standalone S3 exports.

These files document earlier experiments. For the current repeated validation,
use `results/baseline/external_validation/` and
`results/proxy/external_validation/`.

