# Workflow configurations

These files document the canonical inputs and outputs for the two experimental
conditions. They are intended as an unambiguous record for humans; current scripts
receive the same values through command-line arguments.

- `baseline.json`: training without proxy peptides.
- `proxy.json`: training with the cleaned proxy-integrated dataset.

